# JSON access log. JA3 / JA4 / ASN are TLS-handshake + network attributes that
# this proxy can't compute itself (it serves plain HTTP on :80; TLS is
# terminated at the edge / Cloudflare). They're read from request headers that
# the edge injects. With Cloudflare, add HTTP-request Transform Rules:
#
#   X-JA3  =  cf.bot_management.ja3_hash     (Bot Management add-on)
#   X-JA4  =  cf.bot_management.ja4          (Bot Management add-on)
#   X-ASN  =  ip.src.asnum                   (string)
#   (optional) X-Country = ip.src.country, X-Client-IP = cf.connecting_ip
#
# If a header isn't sent, the field logs "" — harmless.
log_format json_combined escape=json '{'
    '"time":"$time_iso8601",'
    '"remote_addr":"$remote_addr",'
    '"client_ip":"$http_cf_connecting_ip",'
    '"method":"$request_method",'
    '"uri":"$uri",'
    '"query":"$query_string",'
    '"status":$status,'
    '"bytes_sent":$bytes_sent,'
    '"body_bytes_sent":$body_bytes_sent,'
    '"request_time":$request_time,'
    '"upstream_response_time":"$upstream_response_time",'
    '"upstream_addr":"$upstream_addr",'
    '"ja3":"$http_x_ja3",'
    '"ja4":"$http_x_ja4",'
    '"asn":"$http_x_asn",'
    '"country":"$http_cf_ipcountry",'
    '"http_referer":"$http_referer",'
    '"http_user_agent":"$http_user_agent"'
'}';
